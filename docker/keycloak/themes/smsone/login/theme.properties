parent=base
# ?v= busts browser caches: KC serves theme resources under a constant
# /resources/<build-version>/ path with max-age=30d, so WITHOUT a version
# bump every returning browser keeps stale CSS for a month after a theme
# change. Bump the value whenever css/js content changes.
styles=css/smsone.css?v=20260803b
locales=en
meta=viewport==width=device-width,initial-scale=1
kcLogoLink=https://smsone.co.ug
kcFavIcon=/img/favicon.svg