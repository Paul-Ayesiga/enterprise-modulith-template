# The gateway's ADMIN (management) base URL — where the catalog + OpenAPI endpoints live.
# Defaults to the gateway's admin port. Copy to .env.local to override.
GATEWAY_ADMIN_URL=http://localhost:9090
